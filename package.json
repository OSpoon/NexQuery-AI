{
    "name": "nexquery-ai-monorepo",
    "version": "1.0.0",
    "private": true,
    "license": "AGPL-3.0",
    "scripts": {
        "dev": "pnpm -r run dev",
        "build": "pnpm -r run build",
        "install:all": "pnpm install -r",
        "lint": "pnpm -r run lint",
        "type-check": "pnpm -r run type-check",
        "postinstall": "pnpm --filter @nexquery/shared build"
    },
    "workspaces": [
        "backend",
        "frontend"
    ]
}