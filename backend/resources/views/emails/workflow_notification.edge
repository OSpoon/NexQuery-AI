<!DOCTYPE html>
<html>
  <head>
    <style>
      body {
        font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
        color: #333;
        line-height: 1.6;
      }
      
      .container {
        max-width: 600px;
        margin: 0 auto;
        border: 1px solid #e2e8f0;
        border-radius: 8px;
        overflow: hidden;
      }
      
      .header {
        background-color: #ecfdf5;
        padding: 20px;
        border-bottom: 1px solid #a7f3d0;
      }
      
      .header-rejected {
        background-color: #fef2f2;
        border-bottom: 1px solid #fecaca;
      }
      
      .content {
        padding: 24px;
      }
      
      .footer {
        background-color: #f1f5f9;
        padding: 16px;
        font-size: 12px;
        color: #64748b;
        text-align: center;
      }
      
      .status-badge {
        display: inline-block;
        padding: 4px 12px;
        border-radius: 9999px;
        font-size: 12px;
        font-weight: 600;
        margin-bottom: 8px;
      }
      
      .status-approved {
        background-color: #dcfce7;
        color: #16a34a;
      }
      
      .status-rejected {
        background-color: #fee2e2;
        color: #dc2626;
      }
      
      .info-box {
        background-color: #f8fafc;
        border: 1px solid #e2e8f0;
        border-radius: 6px;
        padding: 16px;
        margin: 16px 0;
      }
      
      .info-row {
        display: flex;
        margin-bottom: 8px;
      }
      
      .info-label {
        font-weight: 600;
        color: #475569;
        width: 120px;
        flex-shrink: 0;
      }
      
      .info-value {
        color: #1e293b;
      }

      .reason-box {
        background-color: #fef2f2;
        border: 1px solid #fecaca;
        border-radius: 6px;
        padding: 16px;
        margin: 16px 0;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="header {{ isRejected ? 'header-rejected' : '' }}">
        <span class="status-badge {{ isRejected ? 'status-rejected' : 'status-approved' }}">
          {{ isRejected ? '已拒绝' : '已批准' }}
        </span>
        <h2 style="margin: 8px 0 0 0; color: #1e293b;">
          {{ title }}
        </h2>
      </div>
      <div class="content">
        <p>
          您好，
        </p>
        <p>
          {{ isRejected ? '很抱歉，您的工作流申请已被拒绝。' : '好消息！您的工作流申请已通过全部审批。' }}
        </p>

        <div class="info-box">
          <table style="width: 100%; border-collapse: collapse;">
            <tr>
              <td style="padding: 8px 0; font-weight: 600; color: #475569; width: 120px;">流程实例 ID</td>
              <td style="padding: 8px 0; color: #1e293b; font-family: monospace; font-size: 13px;">{{ processInstanceId }}</td>
            </tr>
            <tr>
              <td style="padding: 8px 0; font-weight: 600; color: #475569;">审批状态</td>
              <td style="padding: 8px 0; color: {{ isRejected ? '#dc2626' : '#16a34a' }}; font-weight: 600;">
                {{ isRejected ? '已拒绝' : '已批准' }}
              </td>
            </tr>
          </table>
        </div>

        @if(reason)
        <div class="reason-box">
          <p style="margin: 0 0 8px 0; font-weight: 600; color: #b91c1c;">拒绝原因</p>
          <p style="margin: 0; color: #1e293b;">{{ reason }}</p>
        </div>
        @endif

        <p>
          {{ isRejected ? '如有疑问，请联系审批人了解详情。' : '您现在可以继续执行相关操作。' }}
        </p>
        <p style="margin-top: 32px;">
          顺颂商祺，<br />NexQuery AI 团队
        </p>
      </div>
      <div class="footer">
        此邮件由 NexQuery AI 工作流系统自动发送，请勿直接回复。
      </div>
    </div>
  </body>
</html>
